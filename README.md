# Credit-Card-Fraud-Detection
Credit Card Fraud Detection using Unsupervised Learning  This project explores unsupervised techniques to detect fraudulent credit card transactions.  We use clustering (K-Means) and anomaly detection (Isolation Forest) and Dbscan models on a dataset of 1 million anonymized transactions.
